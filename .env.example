# The optimization AI is local and does not require an external API key.
# Optional server configuration.
PORT="3000"
MAX_CODE_LENGTH="100000"

# Optional public application URL for deployment integrations.
APP_URL="http://localhost:3000"
